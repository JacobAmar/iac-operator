apiVersion: "terraform.iac.operator/v1"
kind: TfProject
metadata:
  name: ronaldo-tfp
spec:
  path: "terraform/"
  repository: "blabla.com/bla"
  backend: dev
  interval: 30
  variables:
    region: "us-east-1"